import {Container} from "react-bootstrap";
import {PageLayout, RegisterForm} from '../components'

export default () => {

    return (
        <PageLayout>
            <RegisterForm />
        </PageLayout>
    )
}