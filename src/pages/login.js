import {Container} from "react-bootstrap";
import {PageLayout, LoginForm} from '../components'

export default () => {

    return (
        <PageLayout>
            <LoginForm />
        </PageLayout>
    )
}